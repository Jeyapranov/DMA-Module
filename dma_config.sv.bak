class dma_config extends uvm_object;
  `uvm_object_utils(dma_config)
  
  virtual dma_if vif;
  uvm_active_passive_enum is_active = UVM_ACTIVE;

  function new(string name = "dma_config"); super.new(name); endfunction
endclass
